{
  // Solution file — owns no source of its own. `tsc --build` walks these
  // references and checks each project under its own runtime's rules, exactly
  // once. Per-runtime settings live in the referenced configs, not here.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.server.json" },
    { "path": "./tsconfig.test.json" }
  ]
}
